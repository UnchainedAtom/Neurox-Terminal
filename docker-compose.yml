version: '3.8'

services:
  neurox-terminal:
    build: .
    container_name: neurox-terminal-api
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: development
      FLASK_DEBUG: "False"
      DEMO_MODE: "True"  # Set to False if connecting to real Home Assistant
      FLASK_HOST: 0.0.0.0
      FLASK_PORT: 5000
      # Uncomment and set these to connect to real Home Assistant
      # HOME_ASSISTANT_URL: http://homeassistant:8123
      # HOME_ASSISTANT_TOKEN: your_token_here
      # LIGHT_ENTITY_ID: light.your_light
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - neurox-network

networks:
  neurox-network:
    driver: bridge
